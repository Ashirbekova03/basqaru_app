import 'package:flutter/material.dart';

class AppColor {
  static const primary = Color.fromRGBO(35, 134, 54, 1);
  static const secondary = Color.fromRGBO(75, 196, 98, 1);
  static const sent = Color.fromRGBO(237, 102, 102, 1);
  static const receive = Color.fromRGBO(17, 228, 38, 1);
  static const text = Colors.black;
  static const secondaryText = Color.fromRGBO(0, 0, 0, 0.4);
  static const inactive = Color.fromRGBO(0, 0, 0, 0.4);
  static const border = Color.fromRGBO(0, 0, 0, 0.1);
  static const background = Color.fromRGBO(238, 238, 238, 1);
}